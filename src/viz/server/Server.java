package viz.server;

/** Replaced with the real server in the server task. */
public final class Server {
    private Server() {}
    public static void start(int preferredPort, boolean open) throws java.io.IOException {
        throw new UnsupportedOperationException("server not built yet — use: viz.Main trace <file> <inputs>");
    }
}
